apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-ingress
  namespace: argocd
spec:
  entryPoints:
    - web

  routes:
    - match: Host(`argo.emc.staging`)
      kind: Rule
      services:
        - name: argocd-server
          port: 80
