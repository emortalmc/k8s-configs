apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute

metadata:
  name: grafana-ingress
  namespace: monitoring

spec:
  entryPoints:
    - web

  routes:
    - kind: Rule
      match: Host(`metrics.emc.staging`)
      services:
        - name: prom-stack-grafana
          port: 80
